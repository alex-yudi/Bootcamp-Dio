// Variáveis e Constantes
    // Variáveis -> valores que pode ser alterados dentro do código
            var nomeVariavel = valorDeclarado;

    // Constantes -> item que possui um valor declarado que não pode ser alterado. Costuma-se declará-la usando letras maiúsculas
            const CONSTANTE = valorDeclarado;


// Função
        function nomeFuncao() {
            //código utilizado para a função 
        }

        // Para "chamar" uma função (fazer ela ser executada no código), preciso usar o nome dela, seguido dos parênteses e, caso ela tenha parâmetros(valores dentro dos parênteses), adicionar os parâmetros também
                nomeFuncao() //Ela será executada.


// Console
        // Pode ser utilizado tanto no navegador web quanto no terminal, através no node.js
                                // Para abrir o terminar no vsCode é só ir em terminal, na parte superior e selecionar "novo terminal"


console.log("Olá console");

























